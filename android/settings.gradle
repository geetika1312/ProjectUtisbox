pluginManagement {
    includeBuild("../node_modules/@react-native/gradle-plugin")
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven { url = uri("../node_modules/@react-native/gradle-plugin") }
    }
}

plugins {
    id("com.facebook.react.settings")
}

reactSettings {
    autolinkLibrariesFromCommand()
}

rootProject.name = "utisbox"
include(":app")
